# Enable UART console and all boot logs (overrides ESP32 soc defaults).
CONFIG_CONSOLE=y
CONFIG_SERIAL=y
CONFIG_UART_CONSOLE=y
CONFIG_BOOT_BANNER=y

# MCUboot debug logging — needs LOG, but LOG_MODE_MINIMAL
# (already active) avoids the cbprintf linker issue.
CONFIG_MCUBOOT_LOG_LEVEL_DBG=y